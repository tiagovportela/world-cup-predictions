[build]
command = "npm run build"
publish = "dist"

[functions]
directory = "netlify/functions"

# SPA routing — must come AFTER function handling.
# Netlify serves /.netlify/functions/* before applying this catch-all.
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
